/* import { useEffect, useRef } from 'react'
// @ts-ignore
import { PhaserGame } from '../../components/JellioGame/PhaserGame'
import Phaser from 'phaser';
const JellioGardenScreen = () => {
   

    
    //  References to the PhaserGame component (game and scene are exposed)
    const phaserRef = useRef();


    const onCurrentActiveScene = (scene) => {
    
        // This is invoked

    }

 
    return (
        <div>
            <h1>Welcome to My Phaser Game</h1>
            <div id="phaser-container">
                <PhaserGame ref={phaserRef} onCurrentActiveScene={onCurrentActiveScene} />
            </div>
        </div>

    )
}

export default JellioGardenScreen */